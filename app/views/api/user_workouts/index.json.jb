@user_workouts.map do |user_workout|
  {
    weight: user_workout.weight,
    name: user_workout.user_id
    # reps: user_workout.reps,
    # notes: user_workout.notes,
    # time: user_workout.time
  }
end